# "Model Ready" — paste this into a NEW chat window

Copy everything in the box below as your first message in a new chat, then attach the
next model's master photo folder and say which model it is.

---

We're continuing the **dev.wooldridgeboats.com boat-model photo replacement + gallery rebuild** in the repo `~/Desktop/LOCAL-WEBSITE`. Before doing anything, read my memory note **website-photo-replacement-workflow** and the runbook **`_build/BOAT-MODEL-IMAGE-UPDATES-HANDOFF.md`** — they have the full pipeline. Serve locally with `python3 -m http.server 8811` and verify in the browser pane (screenshots come back blank when the pane is hidden — use DOM/`javascript_tool` geometry as the source of truth; set a desktop viewport with `resize_window`, and `resize_window {preset:"mobile"}` to check phones).

**Trigger — I say "Model Ready! <Model>" and attach its master folder** (OneDrive `…/MASTER-WEBSITE PHOTOS/<MODEL>-WEB/`; the Inboard ones are `… IB-WEB`). Then:

1. **Confirm the slug** (`models/<slug>/` AND `assets/photos/<slug>/`). Set **`PHOTOSLUG`** if the photo dir differs from the page dir (known: page `xlt` / photos `alaskan-xlt`). Check if the page is "bare" (no hero/gallery/photo-data.js → insert the section, see the memory's bare-page recipe) vs old-style (plain `<div class="gallery">` → the builder converts it).
2. **Pre-flight:** `_build/check_model_photos.sh "<folder>"` (exit 0 = safe). Fix obvious source typos IN THE MASTER FOLDER myself (double-dash, `GALLER THUMB`, etc.). Unknown trim → ASK the caption wording, then add it to `%CFG` (build_gallery.pl) + `cfg_disp`/`parse_one` (apply_model_photos.sh) + parse_row (apply_mobile_gallery.sh) + `KNOWN_TRIMS` (validator). **NO-CONFIG model** (no CC/WS/Tiller — the token after the length is the model name, e.g. Scout Widebody) → prefix **`NOCFG=1`** (validator + builder both honor it).
3. **Build (desktop + mobile in ONE command):** `[NOCFG=1] [PHOTOSLUG=<x>] _build/apply_model_photos.sh <slug> "<Display Name>" "<folder>"` — `--dry-run` first. Does: desktop fulls+thumbs+covers+hero, model-page rewrite (hero figcaption + captioned gallery + WB_COVERS), repoint homepage fleet card + compare thumb, provenance, build_gallery, stamp — AND if the master has `<L>-<MODEL>-MOBILE/` folders it auto-runs `apply_mobile_gallery.sh` (portraits → `assets/photos/<slug>/mobile/`, mozjpeg-optimized keep-smaller, 400px thumbs, per-length `WB_MOBILE`). Use the model's REAL site name as the display name.
4. **Fix cross-refs the builder does NOT auto-handle:** broken-image sweep for `assets/photos/<slug>/`; repoint any `lp/*` persona hero → the model's full `hero-*.jpg`, the homepage "Shot in the Field" `a.photocard` → `thumbs/cover-<firstlen>.jpg`, and any homepage **category tile** that borrowed the model's OLD photo (grep the homepage for the old basenames). (Auto-handled: fleet-card `ph`, compare `ph`, provenance, build_gallery, stamp.)
5. **Verify in browser:** desktop — N cover cards (one per length), hero loads with HULL #, gcaps, lightbox caption switches config+hull, 0 broken images, no console errors (the only expected one is the harmless site-wide favicon note — ignore), `<!-- SPECS -->` block untouched; also confirm the whole dark area left/right of the lightbox photo navigates. Mobile (≤700px) — independent portrait set, fixed 4:5 frame (no caption "bump"), flat grid hidden, cover-card count shows the mobile total. Check the model's **video**: some pages carry a wrong/leftover `data-yt` — if it's not this model, swap the `<figure class="vidsec">` for the VIDEO COMING SOON `.vidsoon` block.
6. **Give me the commit command — I push** (your env can't auth to GitHub for push; `git fetch` works). `git fetch` first; if origin is ahead it's the IG bot (`assets/homepage/instagram/feed.json` only) → `git pull --rebase origin main` is clean.

**Mobile galleries (baked into the flow):** desktop folder = desktop gallery, `<L>-<MODEL>-MOBILE/` folder = mobile gallery, **fully independent** (different photos/counts fine). Mobile export spec: **4:5 portrait @ 1080×1350, JPEG ~75%**. Same lightbox as desktop, just fed the portrait set on phones. Keep every mobile shot the same size (the ingest warns on mismatches; the 4:5 frame prevents bumping anyway).

**Done (don't redo):** Alaskan LT, Alaskan, Alaskan XL, Alaskan XLT, Rogue HDPE, Skagit, Sport, Super Sport Drifter, Sportster, Alaskan XL Inboard, Scout, Scout Widebody, Skagit Inboard, Sport Inboard. **Known trims:** cc, ws, tiller, aft-ws (Aft Windshield), cabin, first-responder — plus **`NOCFG=1`** for no-config models. **Hull regex `^[03-9]\d{3}$`** (leading-`0` allowed for concept-boat hulls like `0001`/`0002`; still excludes years like 2026 — and it lives in **FOUR** scripts now: build_gallery.pl, apply_model_photos.sh, check_model_photos.sh, **and apply_mobile_gallery.sh** — keep all four in sync). Desktop lightbox nav zones + site favicon (WB monogram) are done and global. Reusable **2-video layout** `.vid2` (thumbs in `assets/video-thumbs/`) + the **video-wall** recipe for multi-video pages.

Ready — I'll drop the next model.

---
